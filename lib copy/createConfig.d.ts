/**
 * Create `ts-to-effect-schemaect-schema.config.js` file.
 *
 * @param path
 * @returns `true` if the file was created
 */
export declare function createConfig(configPath: string): Promise<boolean>;
